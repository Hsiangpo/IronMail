# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The canonical guide lives in **AGENTS.md** (single source of truth for build/test commands, architecture, and conventions). It is imported below so it loads automatically:

@AGENTS.md
